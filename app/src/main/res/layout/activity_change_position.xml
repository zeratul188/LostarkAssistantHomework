<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="cpViewModel"
            type="com.lostark.lostarkassistanthomework.changeposition.ChangePositionViewModel" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background"
        tools:context=".changeposition.ChangePositionActivity"
        android:orientation="vertical">
        <androidx.appcompat.widget.Toolbar
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/toolBar"
            android:minHeight="?attr/actionBarSize"
            android:background="?attr/colorPrimary"/>
        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <CheckBox
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/chkLevel"
                android:text="높은 레벨 순으로 정렬"
                android:textSize="14dp"
                android:layout_margin="10dp"
                android:textColor="@color/main_font"
                android:background="@drawable/background_checkbox"
                android:drawableStart="@drawable/button_checkbox"
                android:button="@null"
                android:drawablePadding="10dp"
                android:paddingLeft="10dp"
                app:layout_constraintTop_toTopOf="parent"/>
            <androidx.recyclerview.widget.RecyclerView
                android:layout_width="match_parent"
                android:layout_height="0dp"
                app:layout_constraintTop_toBottomOf="@id/chkLevel"
                app:layout_constraintBottom_toTopOf="@id/btnApply"
                app:layout_constraintVertical_weight="1"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="10dp"
                android:id="@+id/listView"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>
            <android.widget.Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/btnApply"
                android:background="@drawable/button_style"
                android:text="적용"
                android:textSize="16dp"
                android:textColor="@color/main_font"
                android:layout_margin="10dp"
                style="?android:attr/borderlessButtonStyle"
                app:layout_constraintBottom_toBottomOf="parent"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
    </LinearLayout>
</layout>